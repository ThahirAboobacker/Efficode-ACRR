# CodeBERT Optimizer Test Report

## Summary Statistics

|:----------------------------|:---------------|
| Total Test Cases            | 16             |
| Optimized Cases             | 11 (68.8%)     |
| Pattern-Based Optimizations | 5 (31.2%)      |
| Neural Optimizations        | 0 (0.0%)       |
| Rule-Based Optimizations    | 6 (37.5%)      |
| Average Optimization Time   | 2.5312 seconds |
| Average Improvement Score   | 4.12/10        |

## Performance by Category

| Category            |   Total Cases | Optimized Cases   | Avg Improvement   |
|:--------------------|--------------:|:------------------|:------------------|
| Sorting Algorithms  |             4 | 4 (100.0%)        | 6.50/10           |
| Search Algorithms   |             3 | 3 (100.0%)        | 5.00/10           |
| Dynamic Programming |             3 | 3 (100.0%)        | 6.00/10           |
| Data Structures     |             2 | 1 (50.0%)         | 3.50/10           |
| General Python Code |             4 | 0 (0.0%)          | 0.00/10           |

## Top 5 Improvements

### 1. Recursive Fibonacci (Dynamic Programming)
- Improvement Score: 8/10
- Method: pattern
- Size Change: 91 bytes (98.9%)
- Complexity: Improved recursion to iteration (better space complexity, avoids stack overflow)

Original Code:
```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
```

Optimized Code:
```python
def fibonacci(n):
    if n <= 0:
        return 0
    if n <= 2:
        return 1
        
    a, b = 1, 1
    for _ in range(3, n + 1):
        a, b = b, a + b
        
    return b

```

### 2. Bubble Sort (Sorting Algorithms)
- Improvement Score: 7/10
- Method: pattern
- Size Change: 604 bytes (293.2%)
- Complexity: Significant algorithmic improvement from quadratic to linearithmic time

Original Code:
```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
```

Optimized Code:
```python
def heap_sort(arr):
    n = len(arr)
    
    # Build max heap
    for i in range(n // 2 - 1, -1, -1):
        heapify(arr, n, i)
    
    # Extract elements from heap one by one
    for i in range(n - 1, 0, -1):
        arr[i], arr[0] = arr[0], arr[i]  # Swap
        heapify(arr, i, 0)
    
    return arr

def heapify(arr, n, i):
    largest = i
    left = 2 * i + 1
    right = 2 * i + 2
    
    # Check if left child exists and is greater than root
    if left < n and arr[left] > arr[largest]:
        largest = left
    
    # Check if right child exists and is greater than largest
    if right < n and arr[right] > arr[largest]:
        largest = right
    
    # Change root if needed
    if largest != i:
        arr[i], arr[largest] = arr[largest], arr[i]  # Swap
        heapify(arr, n, largest)

```

### 3. Selection Sort (Sorting Algorithms)
- Improvement Score: 7/10
- Method: pattern
- Size Change: 42 bytes (16.9%)
- Complexity: Significant algorithmic improvement from quadratic to linearithmic time

Original Code:
```python
def selection_sort(arr):
    n = len(arr)
    for i in range(n):
        min_idx = i
        for j in range(i+1, n):
            if arr[j] < arr[min_idx]:
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr
```

Optimized Code:
```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
        
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    return quick_sort(left) + middle + quick_sort(right)

```

### 4. Insertion Sort (Sorting Algorithms)
- Improvement Score: 7/10
- Method: pattern
- Size Change: 327 bytes (144.7%)
- Complexity: Significant algorithmic improvement from quadratic to linearithmic time

Original Code:
```python
def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
    return arr
```

Optimized Code:
```python
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
        
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    
    return merge(left, right)
    
def merge(left, right):
    result = []
    i = j = 0
    
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i])
            i += 1
        else:
            result.append(right[j])
            j += 1
            
    result.extend(left[i:])
    result.extend(right[j:])
    
    return result

```

### 5. Inefficient Tree Traversal (Data Structures)
- Improvement Score: 7/10
- Method: rule-based
- Size Change: -113 bytes (-20.9%)
- Complexity: Complexity analysis inconclusive

Original Code:
```python
class Node:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None

def count_nodes(root):
    # Inefficient way to count nodes - could be done with a simple traversal
    if root is None:
        return 0
    
    total = 0
    nodes = [root]
    
    while nodes:
        node = nodes.pop(0)  # Inefficient for a list
        total += 1
        
        if node.left:
            nodes.append(node.left)
        if node.right:
            nodes.append(node.right)
    
    return total
```

Optimized Code:
```python
class Node:

    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None
        return


def count_nodes(root):
    if root is None:
        return 0
    total = 0
    nodes = [root]
    while nodes:
        node = nodes.pop(0)
        total += 1
        if node.left:
            nodes.append(node.left)
        if node.right:
            nodes.append(node.right)
    return 0

```

## Detailed Results

Detailed results are available in JSON format at: test_results\detailed_results.json
